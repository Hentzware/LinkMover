<!-- Copyright (c) 2026 Henning Entz
     SPDX-License-Identifier: Apache-2.0 WITH LicenseRef-Commons-Clause
     See LICENSE for the full Apache 2.0 text and Commons Clause restriction. -->
<UserControl x:Class="LinkMover.JunctionCreation.Views.JunctionWizardHostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:nav="clr-namespace:LinkMover.Core.Navigation;assembly=LinkMover.Core"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Loaded="OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="24,16" Background="{DynamicResource MahApps.Brushes.Gray10}" BorderBrush="{DynamicResource MahApps.Brushes.Gray8}" BorderThickness="0,0,0,1">
            <TextBlock Text="Junction erstellen" FontSize="22" FontWeight="SemiBold"/>
        </Border>

        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="{x:Static nav:RegionNames.WizardStep}"/>

        <Border Grid.Row="2" Padding="24,12" BorderBrush="{DynamicResource MahApps.Brushes.Gray8}" BorderThickness="0,1,0,0" Background="{DynamicResource MahApps.Brushes.Gray10}">
            <Grid>
                <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="{DynamicResource LinkMover.Brushes.Muted}"
                           Text="{Binding StepLabel}"/>
                <Button HorizontalAlignment="Right" Content="Abbrechen" Padding="20,6"
                        Command="{Binding CancelCommand}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>

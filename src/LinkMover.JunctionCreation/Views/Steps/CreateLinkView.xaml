<!-- Copyright (c) 2026 Henning Entz
     SPDX-License-Identifier: Apache-2.0 WITH LicenseRef-Commons-Clause
     See LICENSE for the full Apache 2.0 text and Commons Clause restriction. -->
<UserControl x:Class="LinkMover.JunctionCreation.Views.Steps.CreateLinkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Junction erstellen" FontSize="16" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Margin="0,0,0,4">
            <Run Text="Verknüpfe " Foreground="#666"/>
            <Run Text="{Binding Context.Source, Mode=OneWay}" FontFamily="Consolas"/>
        </TextBlock>
        <TextBlock Grid.Row="2" Margin="0,0,0,16">
            <Run Text="auf " Foreground="#666"/>
            <Run Text="{Binding Context.Target, Mode=OneWay}" FontFamily="Consolas"/>
        </TextBlock>

        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Foreground="#666" Margin="0,8,0,0"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0"
                    Visibility="{Binding ShowError, Converter={StaticResource BoolToVis}}">
            <Button Content="Erneut versuchen" Command="{Binding RetryCommand}" Padding="20,6"/>
        </StackPanel>
    </Grid>
</UserControl>
